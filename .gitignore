# Python
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
.eggs/

# Node / Next.js
node_modules/
.next/
out/
.pnpm-store/
next-env.d.ts
*.tsbuildinfo

# Environment (keep .env.example)
.env
.env.*
!.env.example

# Credentials — never commit. GCP service-account keys carry a live private
# key; prefer storing them OUTSIDE the repo entirely and pointing
# GOOGLE_APPLICATION_CREDENTIALS at an absolute path.
*.pem
*.key
service-account*.json
*-service-account.json
gcp-*.json
Project_*.json
credentials*.json

# OS junk
.DS_Store
Thumbs.db
